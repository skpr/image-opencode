name: Build and Push Stable

on:
  push:
    branches: [ releases ]
  workflow_dispatch: ~
  schedule:
    - cron: '0 14 * * *'   # 14:00 UTC daily

jobs:
  build:
    uses: ./.github/workflows/build-push.yml
    with:
      stream: stable
      push: true
    secrets: inherit
